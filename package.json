{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": ">=22",
    "pnpm": "^10.33.4"
  },
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build --force",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "render:build": "pnpm install --frozen-lockfile=false && tsc --build --force && pnpm --filter \"@workspace/thinking-spree\" run build && pnpm --filter \"@workspace/api-server\" run build && pnpm run render:migrate",
    "render:migrate": "node scripts/run-migrations.mjs",
    "render:start": "node --enable-source-maps artifacts/api-server/dist/index.mjs"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "xlsx": "^0.18.5"
  }
}

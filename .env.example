# Copy to .env and fill in.
# DO NOT commit your real .env — it's in .gitignore.

# ─── Required ─────────────────────────────────────────────────────────────
# Postgres connection string (Neon, Render Postgres, or local docker)
DATABASE_URL=postgresql://user:password@localhost:5432/thinkingspree

# Express session secret — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-random-32-byte-hex-string-here

# ─── Optional (but needed for Google integrations) ────────────────────────
# See GOOGLE_INTEGRATION_SETUP.md for how to obtain these.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:5000/api/google/oauth/callback

# Where the OAuth callback redirects the user after success.
# In production: https://app.thinkingspree.com
APP_BASE_URL=http://localhost:5000

# ─── Optional (for AI-generated emails) ───────────────────────────────────
AI_INTEGRATIONS_OPENAI_API_KEY=
AI_INTEGRATIONS_OPENAI_BASE_URL=

# ─── Gemini (AI emails, research, fencing, T-Sheet cleaning) ──────────────
# Server-side key for all Gemini calls. Without it, AI features are disabled.
GEMINI_API_KEY=
# Model tiers (optional overrides). Work is routed by how much reasoning it
# needs: LITE for simple data-pulling (extraction, cleaning, classification,
# summaries), STANDARD for reasoning-heavy work (Fencing/competitive search,
# the AI research assistant, email drafting, grounded market research).
GEMINI_MODEL_LITE=gemini-flash-lite-latest
GEMINI_MODEL_STANDARD=gemini-flash-latest

# ─── Sales · Retargeting (ISB autofill) ───────────────────────────────────
# Master summary sheet + tab that hold the ISB T-Sheet ("Sheet link", col Z)
# and session transcript Google-Doc ("Transcript Gdoc Link", col AA) per
# startup (col B). Used to auto-fill the Analyse step for ISB Cleantech /
# IRA 2.0 companies. Defaults to the ISB summary sheet if unset. The signed-in
# consultant's Google account must have read access to this sheet.
ISB_MAPPING_SHEET_URL=https://docs.google.com/spreadsheets/d/1RXGM5JCijqeBi_0KC31zZwYDU7V30iB3ijvNBHSfMgc/edit
ISB_MAPPING_TAB=For ISB

# ─── Server config ────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=development
